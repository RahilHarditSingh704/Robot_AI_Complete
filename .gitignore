# Secrets
.env

# TLS cert/key (self-signed, generated by tls_cert.py on first run)
certs/

# Downloaded models: Piper TTS voices and the YuNet face detector.
# See models/download.sh and CLAUDE.md's Running section.
models/*.onnx
models/*.onnx.json

# Saved kiosk notes
data/

# Python
venv/
__pycache__/
*.pyc

# Logs
*.log

# Chromium writes this into the unpacked extension when it loads it
kiosk-extension/_metadata/

# Claude Code local settings (machine-specific permissions, not shared)
.claude/settings.local.json
